#if defined(Hiro_Color)

enum class SystemColor : u32 {
  None, // Not a system color
  Text, // Text inside textfields
  Label, // Labels inside dialogs
  Sublabel, // Sublabels inside dialogs
  Link, // Clickable links
  PlaceholderText, // Greyed out placeholder text
};

#endif
